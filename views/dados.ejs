<%- include('partials/header.ejs') %>
<%- include('partials/menu.ejs') %>

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h3 class="text-center">Meus Dados</h3>
                    </div>
                    <div class="card-body">
                        <div class="text-center mb-4">
                            <% if (funcionario.foto) { %>
                                <img src="/uploads/<%= funcionario.foto %>" alt="Foto de perfil" class="rounded-circle" style="width: 150px; height: 150px; object-fit: cover;">
                            <% } else { %>
                                <img src="/images/perfil-default.png" alt="Foto de perfil padrão" class="rounded-circle" style="width: 150px; height: 150px; object-fit: cover;">
                            <% } %>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label">Matrícula</label>
                                <p class="form-control"><%= funcionario.matricula %></p>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Nome Completo</label>
                                <p class="form-control"><%= funcionario.nome %> <%= funcionario.sobrenome %></p>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label">Temperatura Preferida</label>
                                <p class="form-control"><%= funcionario.temp_preferida %>°C</p>
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Luminosidade Preferida</label>
                                <p class="form-control"><%= funcionario.lumi_preferida %> lux</p>
                            </div>
                        </div>

                        <% if (funcionario.tag_nfc) { %>
                            <div class="mb-3">
                                <label class="form-label">Tag NFC</label>
                                <p class="form-control"><%= funcionario.tag_nfc %></p>
                            </div>
                        <% } %>

                        <div class="text-center">
                            <a href="/preferencias" class="btn btn-primary">Gerenciar Preferências</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

<%- include('partials/footer.ejs') %>
<%- include('./partials/vlibras.ejs') %>
</body>