<div class="modal fade" id="confirmLogoutModal" tabindex="-1" aria-labelledby="confirmLogoutModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded-custom shadow-custom">
            <div class="modal-header border-0">
                <h5 class="modal-title fw-bold" id="confirmLogoutModalLabel">Confirmar Logout</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body py-4">
                <p class="mb-0 text-center">Tem certeza que deseja sair da sua conta?</p>
            </div>
            <div class="modal-footer border-0">
                <button type="button" class="btn btn-outline-secondary px-4" data-bs-dismiss="modal">Cancelar</button>
                <button id="confirmLogoutButton" type="button" class="btn btn-primary px-4">Confirmar</button>
            </div>
        </div>
    </div>
</div>